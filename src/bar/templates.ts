const tplTabButton = (text: string, index: number) => `
<div id="joco-bar-item-${index}" class="joco-bar-item">${text}</div>
`;

// const tplTabButton = (text: string, index: number) => `
// <div id="joco-bar-item-${index}" class="joco-bar-item"><span>${text}</span></div>
// `;
